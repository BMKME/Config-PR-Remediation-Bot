# Config-to-PR Bot Environment Variables
# For PoC demonstration - replace with real tokens for actual use

# GitHub Configuration (required for PR creation)
GITHUB_TOKEN=demo_token_replace_with_real_token

# GitLab Configuration (optional)
GITLAB_TOKEN=demo_token_replace_with_real_token
GITLAB_URL=https://gitlab.com

# Application Configuration
FLASK_ENV=development
FLASK_DEBUG=True
LOG_LEVEL=INFO

# Database Configuration (optional for PoC)
DATABASE_URL=sqlite:///config_to_pr_bot.db

